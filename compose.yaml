services:
  postgres:
    image: 'postgres:14.1'
    environment:
      - 'POSTGRES_PASSWORD=docker'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
